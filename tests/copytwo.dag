
// 2D copy kernel to test ranges, dim() calls, and indexing.
// This is a lot harder than it looks. 

int[][] copyTwo(int[][] input){
  return for (int i : range(0, dim0(input), 1)){
    return for (int j : range(0, dim1(input), 1)){
      return input[i][j];
    };
  };
}
